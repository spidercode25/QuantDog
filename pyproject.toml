# Ruff configuration for QuantDog

[tool.ruff]
target-version = "py310"

[tool.ruff.lint]
select = ["E9", "F63", "F7", "F82"]
exclude = [
    ".sisyphus",
    "logs",
    "typings",
    "**/__pycache__",
    ".venv",
]
